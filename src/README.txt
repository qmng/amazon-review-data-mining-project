setupFiles creates train and test .csv files

to launch project.py:

python3 project.py <model> <product_name> <num Epochs> <alpha> <delta if cluster>

testCluster.sh script runs for all products individually with cluster model and writes results in *Result.txt file.
